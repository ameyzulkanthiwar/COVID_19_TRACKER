import React from "react";

const Cards = (props) => {
    console.log("Props:");
    console.log(props);
    return (
        <div>
            <h1>Card</h1>
        </div>
    );
};

export default Cards;
